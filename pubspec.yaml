name: lkapp
description: 轻之国度 lightnovel.fun 第三方客户端(基础版)
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  shared_preferences: ^2.2.2
  cached_network_image: ^3.3.1
  cupertino_icons: ^1.0.6
  wakelock_plus: ^1.2.5
  flutter_open_chinese_convert: 0.9.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
